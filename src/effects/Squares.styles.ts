import styled from '@emotion/styled';

export const StyledCanvas = styled.canvas`
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;  
  height: 100vh;
  display: block;
  border: none;
  z-index: -1;
  opacity: 0.5;
`;
